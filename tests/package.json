{
  "name": "gltf-blender-io-tests",
  "version": "1.0.0",
  "description": "Test suite for glTF-Blender-IO",
  "devDependencies": {
    "eslint": "^9.3.0",
    "gltf-validator": "^2.0.0-dev.3.10",
    "mocha": "^10.4.0",
    "mochawesome": "^7.1.3",
    "pngjs": "^7.0.0"
  },
  "scripts": {
    "test": "mocha --reporter mochawesome",
    "test-bail": "mocha -b --reporter mochawesome",
    "lint": "eslint **/*.js",
    "lint:fix": "eslint --fix **/*.js"
  },
  "mocha": {
    "timeout": 60000
  }
}
